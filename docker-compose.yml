version: '3.4'
services:
  frontend:
    build:
      context: .
    container_name: "blazar-frontend"
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "3000:3000"
    command: npm run dev